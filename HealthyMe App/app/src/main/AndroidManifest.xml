<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanket.healthyme">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="@string/step_detector"
        android:required="false" />
    <uses-feature
        android:name="@string/step_counter"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthAcivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ResetPasswordActivty" />
        <activity android:name=".HealthyActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".CalorieCalculated" />
        <activity android:name=".selectFood" />
        <!--<activity android:name=".MainActivity" />!-->
        <activity android:name=".DisplayFoodActivity" />
        <activity android:name=".FoodItemDetail"></activity>
    </application>

</manifest>